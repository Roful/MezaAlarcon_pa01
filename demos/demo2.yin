(define fact
  (fun ([x Int] [-> Int])
    (if (= x 0) 1 (* x (fact (- x 1))))))

(fact 5)                                -- 120






(define odd

 (fun ([x Int]
       [-> Float])

    (if (= x 0) false
        (if (= x 1)
            (even (- x 1))
            true))))






(define even
  (fun ([x Int] [-> Bool])
    (if 